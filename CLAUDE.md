# ⚠️ このリポジトリは生成物です。編集しないでください

ここに置かれているファイルは、**上流リポジトリから自動デプロイで生成された公開用のコピー**です。
このリポジトリで直接編集しても、次のデプロイで上流の内容に上書きされて消えます。

修正は**上流リポジトリ側**で行ってください。上流の場所が分からない場合は、
このクローンの `.claude/mirror-upstream.txt`（ローカル専用・リポジトリには含まれません）
を参照するか、リポジトリ管理者に確認してください。

## 生成されるもの

| 対象 | 扱い |
|---|---|
| ルート直下の `*.html` | 上流から生成。編集不可 |
| `business-skills/` | 同上 |
| `*-mirror/` | 同上 |
| `scripts/` ・ `.claude/` | このクローン固有。編集してよい |

## 機械的な保護

`.claude/hooks/mirror_guard.py` が、生成物への Write / Edit / Bash を PreToolUse で拒否します。
デプロイ機構自体の修理などで本当に必要なときだけ `MIRROR_GUARD_OFF=1` を付けて実行してください。

    python .claude/hooks/mirror_guard.py --selftest

## なぜこの警告があるか

このリポジトリが生成物であると気づかずに直接編集され、その変更が上流に反映されないまま
失われかけたことがあります。ドキュメントだけでは読み飛ばされたため、機械側でも止めています。
